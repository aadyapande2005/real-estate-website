1) Fix the getUsers function in the user.route, don't know why it even exists
2) Fix the AuthContext, change it to token based auth instead of localStorage
3) Loaders of ProfilePage causing error if not signed in (extends from Bug 2)